#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include <iostream>

using namespace std;

int Atoi(const char* str) {
  // 错误处理
  // 检查输入(NULL, "")
  // 符号
  // 非法字符
  // 越界
}

int main() {
  return 0;
}
